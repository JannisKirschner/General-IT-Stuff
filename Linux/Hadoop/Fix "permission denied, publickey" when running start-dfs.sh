You have a problem with the ssh/permissions

-First login as root
-Then either create an ssh keypair or enable password based auth
-Enable root per ssh

(ssh localhost must work)

Then it should be fixed
